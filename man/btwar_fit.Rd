% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/btwar_fit.R
\name{btwar_fit}
\alias{btwar_fit}
\title{Fit a Butterworth-Induced Autoregressive Model}
\usage{
btwar_fit(
  y_tr_raw,
  y_te_raw,
  fs = 2,
  method = "ls",
  As_vec = seq(20, 60, by = 5),
  N_vec = 2:20,
  max_d = 3L,
  alpha_stationarity = 0.05,
  min_train = NULL,
  verbose = TRUE
)
}
\arguments{
\item{y_tr_raw}{Numeric vector. Training time series.}

\item{y_te_raw}{Numeric vector. Test (hold-out) time series.}

\item{fs}{Positive numeric. Sampling frequency. Default \code{2}.}

\item{method}{Character string. Scaling estimation method, one of
\code{"ls"} (least squares), \code{"lad"} (least absolute deviations),
or \code{"huber"} (Huber M-estimator). Default \code{"ls"}.}

\item{As_vec}{Numeric vector. Grid of stopband attenuation values (dB)
to search over. Default \code{seq(20, 60, by = 5)}.}

\item{N_vec}{Integer vector. Grid of AR orders to search over.
Default \code{2:20}.}

\item{max_d}{Non-negative integer. Maximum differencing order allowed
during the stationarity transformation step. Default \code{3}.}

\item{alpha_stationarity}{Numeric in \eqn{(0, 1)}. Significance level
for the Augmented Dickey-Fuller stationarity test. Default \code{0.05}.}

\item{min_train}{Non-negative integer or \code{NULL}. Minimum number of
observations used for training in the rolling-origin cross-validation
and for the final predictions. If \code{NULL} (default), the criterion
\code{max(20 * p, 100)} is applied automatically.}

\item{verbose}{Logical. If \code{TRUE} (default), prints progress messages
during cross-validation.}
}
\value{
An object of class \code{"btwar"}, which is a named list
containing:
\describe{
\item{\code{parameters}}{List with \code{d_used}, \code{N_opt},
\code{A_opt}, \code{fc_opt}, \code{alpha}, and \code{phi}.}
\item{\code{performance}}{List with \code{rmse_train} and
\code{rmse_test}.}
\item{\code{train}}{List with \code{y_real} and \code{yhat} for
the training set.}
\item{\code{test}}{List with \code{y_real} and \code{yhat} for
the test set.}
\item{\code{mu}}{Training mean used for centering.}
\item{\code{call}}{The matched call.}
}
}
\description{
Fits a Butterworth-Induced Autoregressive (BTWAR) model using
nested rolling-origin cross-validation for order and attenuation
parameter selection.
}
\examples{
\dontrun{
result <- btwar_fit(
  y_tr_raw = train_series,
  y_te_raw = test_series,
  fs       = 2,
  method   = "ls"
)
summary(result)
}

}
\seealso{
\code{\link{predict.btwar}}, \code{\link{summary.btwar}}
}
